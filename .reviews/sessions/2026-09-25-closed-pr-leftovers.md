# 2026-09-25 — closed-unmerged leftovers

Open PRs: none.

Closed without merge since 2026-09-01, not already on `main`:

- #104 triage record
- #105 score refresh (plus a current refresh so freshness stays inside the fail window)
- #79 palette learning only; loading-state behavior already on `main`
- #111 uv-build floor `>=0.12.13,<0.13`
- #102 leftovers only: `GET /api/plugins/{plugin_id}` and `/api/integrations/{name}/...` aliases

Already on `main`: #89 (httpcore2 2.12.0), #103 (Studio-corrected architecture doc `8f4f06e`), #109 (setup-uv v10.1.0), #110 (mcp 2.2.0, ruff 0.16.8), #115 (anyio 4.14.2).
